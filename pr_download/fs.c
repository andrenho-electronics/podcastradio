#include "fs.h"

#include <stdlib.h>

void
fs_init()
{
}

char*
fs_download_file(char* url, char* error_desc)
{
    (void) url; (void) error_desc;
    return NULL;
}

void
fs_remove_file(char* filename)
{
    (void) filename;
}
